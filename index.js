const { addFooter } = require("./lib/add-footer")
const { addHeader } = require("./lib/add-header")
const { magicString } = require("./lib/magic-string")
const { replace } = require("./lib/replace")

module.exports = {
  addFooter,
  addHeader,
  magicString,
  replace,
}
